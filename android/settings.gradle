pluginManagement {
    repositories {
        mavenCentral()
        google()
        gradlePluginPortal()
    }
    if (file("../node_modules/react-native-gradle-plugin").exists()) {
        includeBuild("../node_modules/react-native-gradle-plugin")
    }
}

dependencyResolutionManagement {
    repositories {
        mavenCentral()
        google()
    }
}

rootProject.name = 'MyTabbedApp'
include(":app")
